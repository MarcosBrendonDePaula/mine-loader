pluginManagement {
    repositories {
        maven { name = 'Fabric'; url = 'https://maven.fabricmc.net/' }
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'minecraft-lua-loader'

// Nucleo agnostico de plataforma. O modulo raiz e o adaptador Fabric.
include 'core'
